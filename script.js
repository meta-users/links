$(function(){
    // ======= word "OR" Center
    var apsWidth = $('.aps p').width();
    var realWidth = 'calc(50% - (' + apsWidth + 'px / 2))';
    $('.aps p').css('left',realWidth);
    //======================

   

});
